!!!5
%html
  %head
    %title Blogme
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
    %link{:href => "http://fonts.googleapis.com/css?family=Oswald", :rel => "stylesheet", :type => "text/css"}/
  %body{:id => "application", :class => controller.controller_name}
    %header.navbar
      .container
        .row.text-uppercase
          .col-sm-4.hidden-xs
            %ul.list-inline.text-right
              %li= link_to 'Home', home_path, class: 'active'
              - @categories.each do |c|
                %li= link_to c.name, category_path(category: c.name)
          .col-sm-3.col-xs-12
            .logo
              = image_tag('logo.png', class: 'img-responsive center-block')
          .col-sm-4.hidden-xs
            %ul.list-inline.text-left
              - @pages.each do |p|
                %li= link_to p.name, page_path(name: p.name)

    / Content

    #content.container
      .row
        .main-content.col-sm-9
          = yield

        %aside.sidebar.col-sm-3
          - if @author
            = render @author

          %ol.popular-posts
            %h4.text-center Popular posts
            -@popular_posts.each do |pp|
              %li=link_to pp.title, post_path(pp.id)
          %ol.recent-posts
            %h4.text-center Recent posts
            - @recent_posts.each do |po|
              %li= link_to po.title, post_path(po.id)
          .archive
            %h4.text-center Archive
            - @archive.each_pair do |month, posts|
              %div{class: "#{month.split(' ').join('-')}"}
                %h5.expand-btn.highlightable
                  = "#{month}"
                  %small="(#{posts.length})"
                  %span.caret
                %ul.monthly-posts
                  - posts.each do |post|
                    %li.hidden= link_to post[0], post_path(id:post[1])
          / TODO
          / .suscribe-form

    / Footer

    %footer
      .container
        .row
          .col-sm-4.col-xs-4
            %h4 Categories
            %ul
            - @categories.each do |c|
              %li= link_to c.name, category_path(category: c.name)
          .col-sm-4.col-xs-4
            %h4 Pages
            %ul
            - @pages.each do |pa|
              %li= link_to pa.name, page_path(name: pa.name)
          .col-sm-4.col-xs-4
            %h4 Other links
        .row
          .col-sm-6
            %p Copyright © 2015 Blogme. All rights reserved.
          .col-sm-6
            %p Social links